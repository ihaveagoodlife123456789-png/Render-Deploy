{
  "dependencies": {
    "concurrently": "^10.0.5"
  },

  "scripts": {
    "start": "npx --yes concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Backend && npm run dev",
    "client": "cd Frontend && npm run dev"
  }
}
