{
  "dependencies": {
    "concurrently": "^10.0.5"
  },

  "scripts": {
    "start": "node ./node_modules/concurrently/dist/bin/concurrently.js \"npm run server\" \"npm run client\"",
    "server": "cd Backend && npm run dev",
    "client": "cd Frontend && npm run dev"
  }
}
